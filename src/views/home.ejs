<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="utf-8">
  <title>its-forms</title>
</head>
<body>
  <header>
    <strong>its-forms</strong>
    —
    <%= user.name %> (<%= user.department ? user.department.name : '-' %>)
    <form method="POST" action="/auth/logout" style="display:inline">
      <button type="submit">ออกจากระบบ</button>
    </form>
  </header>
  <p>ยินดีต้อนรับ!</p>
</body>
</html>
